targets:
  srl:
    address: <SRL_MGMT_IP>:57400
    username: admin
    password: NokiaSrl1!
    skip-verify: true

subscriptions:
  srl-stats:
    paths:
      - /platform/control/cpu[index=all]/total
      - /interface[name=mgmt0]/statistics
    mode: stream
    stream-mode: sample
    sample-interval: 2s

outputs:
  prom:
    type: prometheus
    listen: ":9804"
